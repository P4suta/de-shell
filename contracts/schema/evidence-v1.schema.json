{
  "$defs": {
    "guarantee": {
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "level": { "const": "native" },
            "semantic_model": { "minLength": 1, "type": "string" }
          },
          "required": ["level", "semantic_model"],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "level": { "const": "delegated" },
            "reason": { "minLength": 1, "type": "string" }
          },
          "required": ["level", "reason"],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "level": { "const": "residual" },
            "reason": { "minLength": 1, "type": "string" }
          },
          "required": ["level", "reason"],
          "type": "object"
        }
      ]
    },
    "observation": {
      "additionalProperties": false,
      "allOf": [
        {
          "if": { "properties": { "status": { "enum": ["verified", "different"] } }, "required": ["status"] },
          "then": { "properties": { "digest": { "pattern": "^[0-9a-f]{64}$", "type": "string" } } }
        }
      ],
      "properties": {
        "digest": { "type": ["string", "null"] },
        "key": {
          "additionalProperties": false,
          "properties": {
            "provider_fingerprint": { "pattern": "^[0-9a-f]{64}$", "type": "string" },
            "runtime_lock_digest": { "pattern": "^[0-9a-f]{64}$", "type": "string" },
            "scenario_digest": { "pattern": "^[0-9a-f]{64}$", "type": "string" }
          },
          "required": ["scenario_digest", "provider_fingerprint", "runtime_lock_digest"],
          "type": "object"
        },
        "provider": { "minLength": 1, "type": "string" },
        "reason": { "type": ["string", "null"] },
        "scenario": { "minLength": 1, "type": "string" },
        "status": { "enum": ["verified", "different", "unavailable", "failed", "nondeterministic"] }
      },
      "required": ["scenario", "key", "status", "provider", "reason", "digest"],
      "type": "object"
    },
    "path": {
      "pattern": "^(?!/)(?!\\.{1,2}(?:/|$))(?!.*//)(?!.*\\/\\.{1,2}(?:/|$))(?!.*\\/$)[^\\\\:\\u0000]+$",
      "type": "string"
    }
  },
  "$id": "https://deshell.dev/contracts/evidence/v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "nodes": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "guarantee": { "$ref": "#/$defs/guarantee" },
          "id": { "pattern": "^[0-9a-f]{32}$", "type": "string" },
          "operation": { "minLength": 1, "type": "string" }
        },
        "required": ["id", "operation", "guarantee"],
        "type": "object"
      },
      "type": "array"
    },
    "observations": {
      "items": { "$ref": "#/$defs/observation" },
      "type": "array"
    },
    "plan_digest": { "pattern": "^[0-9a-f]{64}$", "type": "string" },
    "schema_version": { "const": 1 },
    "source": {
      "additionalProperties": false,
      "properties": {
        "content_hash": { "pattern": "^[0-9a-f]{64}$", "type": "string" },
        "path": { "$ref": "#/$defs/path" }
      },
      "required": ["path", "content_hash"],
      "type": "object"
    }
  },
  "required": ["schema_version", "plan_digest", "source", "nodes", "observations"],
  "title": "de-shell Evidence v1",
  "type": "object"
}
